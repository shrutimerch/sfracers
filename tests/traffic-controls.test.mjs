import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {mappedControls} from '../app/traffic-controls.ts';
const raw=JSON.parse(readFileSync(new URL('../public/traffic-controls-osm.json',import.meta.url)));const d=JSON.parse(readFileSync(new URL('../public/race-course.json',import.meta.url)));
test('traffic control models preserve source IDs, types and exact projected source positions',()=>{const controls=mappedControls(raw.elements,d.roads);assert.equal(controls.length,raw.elements.length);assert.ok(controls.some(c=>c.node.tags.highway==='stop'));assert.ok(controls.some(c=>c.node.tags.highway==='traffic_signals'));for(const c of controls){const source=raw.elements.find(n=>n.id===c.node.id);assert.ok(source);assert.equal(c.x,(source.lon+122.395)*87900);assert.equal(c.z,(37.786-source.lat)*111200);assert.ok(Number.isFinite(c.angle));}assert.deepEqual(mappedControls([],d.roads),[]);});
